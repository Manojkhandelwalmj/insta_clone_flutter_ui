import 'package:flutter/material.dart';

class GeneralMessagesScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("General Message Screen",style: TextStyle(fontSize: 25),),
      ),
    );
  }
}
