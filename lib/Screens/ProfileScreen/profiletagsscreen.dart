import 'package:flutter/material.dart';
class ProfileTagsScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Tags Screen",style: TextStyle(fontSize: 30),),),
    );
  }

}